# .npmrc file to set npm install flags globally for this project
# Uncomment the option you prefer (but not both at the same time)

# Option 1: Use legacy-peer-deps to ignore peer dependency errors
legacy-peer-deps=true

# Option 2: Use force to bypass all conflicts
# force=true